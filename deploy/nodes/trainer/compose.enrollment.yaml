# First start only. Keep the one-time credential in the fixed secret file below.
services:
  trainer:
    environment:
      RKNODE_ENROLLMENT_TOKEN_FILE: /run/secrets/rknode-enrollment-token
    secrets:
      - rknode-enrollment-token

secrets:
  rknode-enrollment-token:
    file: ./secrets/trainer-enrollment-token
